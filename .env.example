# Formspree form IDs (formspree.io) — create a form for each, then set these
# in your Cloudflare Pages project's environment variables.
# Until set, the contact forms render but show "Formulario en configuración".
NEXT_PUBLIC_FORMSPREE_DINER=
NEXT_PUBLIC_FORMSPREE_RESTAURANT=
